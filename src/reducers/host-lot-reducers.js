export default (state=[], action) => {
  let {type, payload} = action;

  switch(type) {
    case 'CREATE_LOT':
      return
    case 'UPDATE_LOT':
      return
    case 'CREATE_':
      return
    case 'DELETE_LOT':
      return
  }
}